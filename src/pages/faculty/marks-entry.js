import BackButton from "@/components/BackButton";

const FacultyMarksEntry = () => (
  <div className="p-8">
    <BackButton />
    <h1 className="text-2xl font-bold mb-4">Marks & Performance Entry</h1>
    <p>Enter marks and performance data for students here.</p>
  </div>
);

export default FacultyMarksEntry;